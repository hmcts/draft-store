pluginManagement {
  repositories {
    maven {
      url 'https://artifactory.reform.hmcts.net/artifactory/libs-release'
    }
    gradlePluginPortal()
  }
}

rootProject.name = 'draft-store'

include 'swagger'
