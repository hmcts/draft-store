#!groovy

@Library("Infrastructure")

def type = "java"
def product = "draft-store"
def app = "service"
def channel = '#rpe-build-notices'

withPipeline(type, product, app) {
  after('test') {
    sh './gradlew integration --info'
  }

  enableSlackNotifications(channel)
  enableDbMigration()
}
